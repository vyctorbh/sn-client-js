export declare class TokenTests {
    private _head;
    private _payload;
    private createTestToken();
    'Construct token from encoded head and payload has valid serialized values'(): void;
    'Create empty token'(): void;
    'toString should return the original head and payload'(): void;
}
